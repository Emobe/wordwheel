{
  "name": "word-wheel",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build-pack": "bun run packages/tools/src/scowl/build-pack.ts",
    "generate": "bun run packages/tools/src/gen.ts",
    "report": "bun run packages/tools/src/report.ts",
    "ascii": "bun run packages/tools/src/ascii.ts",
    "test": "bun test",
    "typecheck": "tsc -b"
  },
  "devDependencies": {
    "@types/bun": "^1.4.2",
    "typescript": "^5.6.0"
  }
}
