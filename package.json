{
  "name": "wordscapes",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "check": "bun run typecheck && bun run lint && bun test",
    "typecheck": "tsc --noEmit && bun run --cwd apps/mobile typecheck",
    "lint": "biome check . && bun run --cwd apps/mobile lint",
    "gen": "bun run packages/tools/src/gen.ts"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@types/bun": "latest",
    "fast-check": "^3.23.1",
    "typescript": "^5.7.2"
  }
}
